import { Styles } from "../../types";

export const styles: Styles = {
  NotFound: {
    fontSize: "2rem",

    a: {
      color: (theme) => theme.palette.grey[600],
      textDecoration: "none",
      fontSize: "1.2rem",
    },
  },
};
